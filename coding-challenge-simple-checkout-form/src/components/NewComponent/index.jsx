import style from './style.module.sass';

// ! This component exist only for copy-pasting from during initial development
export const NewComponent = ({children}) => <div className={style.newComponent}>{children}</div>;
